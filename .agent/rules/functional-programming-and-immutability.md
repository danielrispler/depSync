---
trigger: always_on
---

Prioritize a functional programming paradigm to ensure safe and predictable code. Avoid mutable state. Strictly use `const` over `let` unless absolutely necessary. Avoid imperative loops (such as `for` or `while`); instead, utilize declarative array methods like `map`, `filter`, `reduce`, and `forEach`. Keep functions pure and side-effect-free wherever possible.